remote_theme: pages-themes/minimal@v0.2.0
plugins:
  - jekyll-remote-theme
title: Pine & Prairie Cabin
description: Your home away from home in Suncadia
show_downloads: false

# Disable GitHub metadata to avoid API calls during local builds
github: [metadata]
